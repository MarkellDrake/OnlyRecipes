<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> Only Recipe's  </title>
    <link rel="stylesheets" href="/public/css/jass.css">
    <link rel="stylesheets" href="/public/css/style.css">
</head>


<body>
    <header class="display-flex justify-space-between align-center p-2">
        <h1>
            &#127881  Only Recipe's   &#127881
        </h1>
  <!-- picture of the recipe of the week -->
    </header>
     <h2> Your Random Recipe !!</h2>
    <nav>
        <a href="/">Home</a>
        {{#if loggedIn}}
         <button id="MyProfile" class="btn-no-style">My Profile</button>
        <button id="Logout" class="btn-no-style">logout</button>
        {{else}}
       <a href="/profile">My Profile</a>
        <a href="/login">Login</a>
        {{/if}}
        
        <!-- searchbar maybe ?!? -->
    </nav>
    <main>
        <aside>
             <h2> Available Recipes! </h2>
              <!-- Implement Title of each  available Recipe to 10  -->
          
        </aside>
      {{{body}}}

    </main>
    {{#if loggedIn}}
    <script src="/public/js/logout.js"> </script>
    {{/if}}
  
</body>


    </html>