<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> Only Recipe's  </title>
    <link rel="stylesheets" href="/public/css/jass.css">
    <link rel="stylesheets" href="/public/css/style.css">
</head>


<body>
    <header class="display-flex justify-space-between align-center p-2">
        <h1>
            &#127881 Your random recipe! &#127881
        </h1>
        <!-- picture of the recipe of the week -->
    </header>
    <nav>
        <a href="/">Home</a>
        {{#if logged}}
        <button id="logout" class="btn-no-style">logout</button>
        {{else}}
        <a href="/login">Login</a>
        {{/if}}
        <!-- searchbar maybe ?!? -->
    </nav>
    <main>
        <aside>
             <h2> Types of recipies! </h2>
              <!-- somehow implement different types of recipes probably cats. -->
        </aside>
      {{{body}}}

    </main>
    <footer>
        
    </footer>
</body>

















    </html>